function process(start_date, end_date) {
     alert("Hello World")
    // $.get("/process?start_date=" + start_date + "&end_date=" + end_date, {}, function (data, status) {
    //         alert("Data: " + data + "\nStatus: " + status);
    // });
}